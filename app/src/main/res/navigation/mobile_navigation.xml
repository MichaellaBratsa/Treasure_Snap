<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.mbrats01.treasuresnap.ui.home.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/action_home_to_camera"
            app:destination="@id/cameraFragment" />

        <action
            android:id="@+id/action_home_to_map"
            app:destination="@id/nav_map" />
    </fragment>

    <fragment
        android:id="@+id/cameraFragment"
        android:name="com.mbrats01.treasuresnap.ui.camera.CameraFragment"
        android:label="Camera"
        tools:layout="@layout/fragment_camera">

        <action
            android:id="@+id/action_camera_to_home"
            app:destination="@id/nav_home" />

        <action
            android:id="@+id/action_camera_to_map"
            app:destination="@id/nav_map" />
    </fragment>

    <fragment
        android:id="@+id/nav_map"
        android:name="com.mbrats01.treasuresnap.ui.map.MapFragment"
        android:label="Treasure Map"
        tools:layout="@layout/fragment_map">

        <action
            android:id="@+id/action_map_to_home"
            app:destination="@id/nav_home" />
    </fragment>

</navigation>
